/*
The for loop below has an error. Fix it!

for (let k = 12 k < 21 k++) {
  console.log(k);
}
*/

/*
 * Programming Quiz: Fix the Error 2
 */
/*
 * QUIZ REQUIREMENTS
 * - Your code should use a `for` loop
 * - Your `for` loop should specify a starting condition `k` with the value of `12`
 * - Your `for` loop should have a stop condition for the value of `k`
 * - Your `for` loop should increment `k` each time it executes
 * - Your code should produce the expected output
 */
 
// fix the for loop
for (let k = 12; k < 21; k++) {
  console.log(k);
}